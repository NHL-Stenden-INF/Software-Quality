services:
  javafx-app:
    build: .
    container_name: javafx_container
    network_mode: "host"
    environment:
      - DISPLAY=host.docker.internal:0.0
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix  
    privileged: true